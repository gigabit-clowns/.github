extends: default

rules:
  truthy:
    check-keys: false

  line-length: disable

  document-start: disable

  indentation:
    spaces: consistent
    indent-sequences: consistent

  comments:
    min-spaces-from-content: 1
